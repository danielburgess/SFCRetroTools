/out/
/*.sfc
/*_ptr_data/

# Python
__pycache__/
*.py[cod]
*.egg-info/
/build/
# setuptools build output inside the per-binary wheel packages (NOT tests/build/,
# which is a real subpackage — see pyproject norecursedirs note).
packages/*/build/
.coverage
.coverage.*
/dist/
Plans/
.venv/
.idea/
*.whl
*.tar.gz

# cibuildwheel
wheelhouse/

# built binaries embedded at wheel-build time (not source)
packages/retrotool-libsfx/retrotool_libsfx/bin/
packages/retrotool-libsfx/retrotool_libsfx/include/
packages/retrotool-libsfx/retrotool_libsfx/examples/
packages/retrotool-xdelta/retrotool_xdelta/bin/
packages/retrotool-asar/retrotool_asar/bin/
# bass: binary + architectures/ + licenses/ are all built/copied from vendor/
# by setup.py at wheel-build time (see retrotool-bass/setup.py).
packages/retrotool-bass/retrotool_bass/bin/
packages/retrotool-bass/retrotool_bass/licenses/

# MemPalace/other local tooling artifacts
*.original.md
